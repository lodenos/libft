#include <stdlib.h>
#include <string.h>
#include "libft.h"

char *ft_strtrim(char const *s1, char const *set) {





//  size_t head; // start
//  size_t tail; // end
//  char *str;

//  if (!s1 || !set)
//    return NULL;
//  head = 0;
//  tail = ft_strlen(s1) - 1;
//  while (ft_strchr(set, s1[head]) && head <= tail)
//    ++head;
//  if (head > tail)
//    return ft_strdup(s1 + tail + 1);
//  while (ft_strchr(set, s1[tail]) && tail >= 0)
//    --tail;
//  str = (char *)malloc(tail - head + 2);
//  if (!str)
//    return NULL;
//  ft_strlcpy(str, &s1[head], tail - head + 2);
//  return str;
}
