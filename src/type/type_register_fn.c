#include "type.h"

t_type_fn *type_register_fn(size_t type, t_type_fn **type_fn) {
//  t_type_register *const reg = type_register();

//  if (!reg || type > reg->size)
  (void)type;
  (void)type_fn;
  return NULL;
//  return reg->fn[type];
}
